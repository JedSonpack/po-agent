# s01 Agent Loop 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在 po-agent 中实现第一阶段——基础 agent loop，行为对齐 `learn-claude-code/s01_agent_loop`，结构规范化（包 + 依赖注入 + TDD）。

**架构：** `s01_agent_loop` 包按职责拆分为 config/tools/agent/cli；`agent_loop` 通过依赖注入接收 `client`/`run_tool`/`on_tool_use`，使其可 mock 单测；cli 负责 REPL 与彩色打印。

**技术栈：** Python 3.11、anthropic SDK、python-dotenv、pytest、subprocess。

**规格：** `docs/superpowers/specs/2026-07-04-s01-agent-loop-design.md`

---

## 文件结构

| 文件 | 职责 | 测试 |
|---|---|---|
| `s01_agent_loop/__init__.py` | 包标识 | — |
| `s01_agent_loop/tools.py` | `run_bash` + 黑名单/超时/截断 | `tests/test_tools.py` |
| `s01_agent_loop/agent.py` | `agent_loop` 核心循环（依赖注入） | `tests/test_agent.py` |
| `s01_agent_loop/config.py` | env 加载、`build_system_prompt`/`make_tools`/`load` | `tests/test_config.py` |
| `s01_agent_loop/cli.py` | REPL + readline + 打印 | 手动冒烟 |
| `s01_agent_loop/__main__.py` | `python -m s01_agent_loop` 入口 | — |
| `s01_agent_loop/tests/__init__.py` | 测试包标识 | — |
| `s01_agent_loop/README.md` | 阶段说明 | — |

所有 pytest 命令从 **po-agent 根目录**运行，使 `s01_agent_loop` 包可导入。前置：`source .venv/bin/activate`。

---

### 任务 1：包骨架 + 安装 pytest

**文件：**
- 创建：`s01_agent_loop/__init__.py`、`s01_agent_loop/tests/__init__.py`

- [ ] **步骤 1：创建包标识文件**

```python
# s01_agent_loop/__init__.py
"""s01_agent_loop — 基础 agent loop（while + tool_use）。"""
```

```python
# s01_agent_loop/tests/__init__.py
```

- [ ] **步骤 2：安装 pytest**

运行：`source .venv/bin/activate && uv pip install pytest`
预期：pytest 装好

- [ ] **步骤 3：验证 pytest 可发现测试目录**

运行：`pytest -q`
预期：`no tests ran`，退出码 5（无测试，正常）

- [ ] **步骤 4：Commit**

```bash
git add s01_agent_loop/
git commit -m "chore(s01): 初始化包骨架与 pytest"
```

---

### 任务 2：tools.run_bash（TDD）

**文件：**
- 创建：`s01_agent_loop/tools.py`、`s01_agent_loop/tests/test_tools.py`

- [ ] **步骤 1：编写失败的测试**

```python
# s01_agent_loop/tests/test_tools.py
import subprocess
from s01_agent_loop.tools import run_bash


def test_dangerous_command_blocked():
    assert run_bash("rm -rf /") == "Error: Dangerous command blocked"
    assert run_bash("sudo ls") == "Error: Dangerous command blocked"


def test_safe_command_returns_output():
    assert run_bash("echo hello") == "hello"


def test_empty_output():
    assert run_bash("true") == "(no output)"


def test_timeout(monkeypatch):
    def fake_run(*args, **kwargs):
        raise subprocess.TimeoutExpired(cmd="sleep", timeout=120)
    monkeypatch.setattr("s01_agent_loop.tools.subprocess.run", fake_run)
    assert run_bash("sleep 200") == "Error: Timeout (120s)"


def test_output_truncation(monkeypatch):
    class FakeResult:
        stdout = "x" * 60000
        stderr = ""
    monkeypatch.setattr("s01_agent_loop.tools.subprocess.run",
                        lambda *a, **k: FakeResult())
    assert len(run_bash("echo big")) == 50000
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pytest s01_agent_loop/tests/test_tools.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 's01_agent_loop.tools'`

- [ ] **步骤 3：编写最少实现代码**

```python
# s01_agent_loop/tools.py
import os
import subprocess

DANGEROUS = ["rm -rf /", "sudo", "shutdown", "reboot", "> /dev/"]
TIMEOUT = 120
MAX_OUTPUT = 50000


def run_bash(command: str) -> str:
    """执行 shell 命令，返回截断后的输出字符串。"""
    if any(d in command for d in DANGEROUS):
        return "Error: Dangerous command blocked"
    try:
        r = subprocess.run(command, shell=True, cwd=os.getcwd(),
                           capture_output=True, text=True, timeout=TIMEOUT)
        out = (r.stdout + r.stderr).strip()
        return out[:MAX_OUTPUT] if out else "(no output)"
    except subprocess.TimeoutExpired:
        return "Error: Timeout (120s)"
    except (FileNotFoundError, OSError) as e:
        return f"Error: {e}"
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pytest s01_agent_loop/tests/test_tools.py -v`
预期：5 passed

- [ ] **步骤 5：Commit**

```bash
git add s01_agent_loop/tools.py s01_agent_loop/tests/test_tools.py
git commit -m "feat(s01): 实现 run_bash 工具（黑名单/超时/截断）"
```

---

### 任务 3：agent.agent_loop（TDD）

**文件：**
- 创建：`s01_agent_loop/agent.py`、`s01_agent_loop/tests/test_agent.py`

- [ ] **步骤 1：编写失败的测试**

```python
# s01_agent_loop/tests/test_agent.py
from types import SimpleNamespace
from s01_agent_loop.agent import agent_loop


def make_response(blocks, stop_reason):
    return SimpleNamespace(content=blocks, stop_reason=stop_reason)


def text_block(t):
    return SimpleNamespace(type="text", text=t)


def tool_use_block(bid, command):
    return SimpleNamespace(type="tool_use", id=bid, input={"command": command})


class FakeMessages:
    def __init__(self, responses):
        self.responses = list(responses)
        self.calls = []

    def create(self, **kwargs):
        self.calls.append(kwargs)
        return self.responses.pop(0)


class FakeClient:
    def __init__(self, responses):
        self.messages = FakeMessages(responses)


def test_no_tool_use_exits_immediately():
    client = FakeClient([make_response([text_block("done")], "end_turn")])
    messages = [{"role": "user", "content": "hi"}]
    agent_loop(client=client, model="m", system="s", tools=[],
               messages=messages, run_tool=lambda c: "")
    assert len(messages) == 2
    assert messages[1]["role"] == "assistant"


def test_tool_use_loop_feeds_result_back():
    client = FakeClient([
        make_response([tool_use_block("t1", "echo hi")], "tool_use"),
        make_response([text_block("all done")], "end_turn"),
    ])
    messages = [{"role": "user", "content": "do it"}]
    seen = []
    agent_loop(client=client, model="m", system="s", tools=[],
               messages=messages, run_tool=lambda c: seen.append(c) or "TOOL_OUT")
    assert len(messages) == 4
    assert messages[2]["role"] == "user"
    assert messages[2]["content"][0] == {
        "type": "tool_result", "tool_use_id": "t1", "content": "TOOL_OUT"}
    assert messages[3]["content"][0].text == "all done"
    assert seen == ["echo hi"]
    assert len(client.messages.calls) == 2


def test_on_tool_use_callback_invoked():
    client = FakeClient([
        make_response([tool_use_block("t1", "ls")], "tool_use"),
        make_response([text_block("ok")], "end_turn"),
    ])
    calls = []
    agent_loop(client=client, model="m", system="s", tools=[],
               messages=[{"role": "user", "content": "x"}],
               run_tool=lambda c: "OUT",
               on_tool_use=lambda cmd, out: calls.append((cmd, out)))
    assert calls == [("ls", "OUT")]
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pytest s01_agent_loop/tests/test_agent.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 's01_agent_loop.agent'`

- [ ] **步骤 3：编写最少实现代码**

```python
# s01_agent_loop/agent.py
"""核心 agent 循环：调工具就继续，不调就停。所有依赖从参数注入。"""
from typing import Callable, Optional


def agent_loop(
    *,
    client,
    model: str,
    system: str,
    tools: list,
    messages: list,
    run_tool: Callable[[str], str],
    max_tokens: int = 8000,
    on_tool_use: Optional[Callable[[str, str], None]] = None,
) -> None:
    while True:
        response = client.messages.create(
            model=model, system=system, messages=messages,
            tools=tools, max_tokens=max_tokens,
        )
        messages.append({"role": "assistant", "content": response.content})
        if response.stop_reason != "tool_use":
            return
        results = []
        for block in response.content:
            if block.type == "tool_use":
                output = run_tool(block.input["command"])
                if on_tool_use:
                    on_tool_use(block.input["command"], output)
                results.append({
                    "type": "tool_result",
                    "tool_use_id": block.id,
                    "content": output,
                })
        messages.append({"role": "user", "content": results})
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pytest s01_agent_loop/tests/test_agent.py -v`
预期：3 passed

- [ ] **步骤 5：Commit**

```bash
git add s01_agent_loop/agent.py s01_agent_loop/tests/test_agent.py
git commit -m "feat(s01): 实现 agent_loop 核心循环（依赖注入）"
```

---

### 任务 4：config（轻量 TDD）

**文件：**
- 创建：`s01_agent_loop/config.py`、`s01_agent_loop/tests/test_config.py`

- [ ] **步骤 1：编写失败的测试**

```python
# s01_agent_loop/tests/test_config.py
import os
from s01_agent_loop.config import build_system_prompt, make_tools, prepare_env


def test_make_tools_is_bash_only():
    tools = make_tools()
    assert len(tools) == 1
    assert tools[0]["name"] == "bash"
    assert tools[0]["input_schema"]["required"] == ["command"]


def test_build_system_prompt_includes_cwd():
    prompt = build_system_prompt("/tmp/x")
    assert "/tmp/x" in prompt
    assert "bash" in prompt


def test_prepare_env_pops_auth_token_when_base_url_set(monkeypatch):
    monkeypatch.setenv("ANTHROPIC_BASE_URL", "https://example.com")
    monkeypatch.setenv("ANTHROPIC_AUTH_TOKEN", "secret")
    prepare_env()
    assert "ANTHROPIC_AUTH_TOKEN" not in os.environ
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pytest s01_agent_loop/tests/test_config.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 's01_agent_loop.config'`

- [ ] **步骤 3：编写最少实现代码**

```python
# s01_agent_loop/config.py
import os
from typing import Any

from anthropic import Anthropic
from dotenv import load_dotenv


def prepare_env() -> None:
    """加载 .env；使用自定义 base_url 时移除 AUTH_TOKEN，避免鉴权冲突。"""
    load_dotenv(override=True)
    if os.getenv("ANTHROPIC_BASE_URL"):
        os.environ.pop("ANTHROPIC_AUTH_TOKEN", None)


def build_system_prompt(cwd: str) -> str:
    return f"You are a coding agent at {cwd}. Use bash to solve tasks. Act, don't explain."


def make_tools() -> list[dict]:
    return [{
        "name": "bash",
        "description": "Run a shell command.",
        "input_schema": {
            "type": "object",
            "properties": {"command": {"type": "string"}},
            "required": ["command"],
        },
    }]


def load() -> dict[str, Any]:
    """读取 env 并组装运行时依赖。仅在 cli 调用时执行，避免导入时读 env。"""
    prepare_env()
    return {
        "client": Anthropic(base_url=os.getenv("ANTHROPIC_BASE_URL")),
        "model": os.environ["MODEL_ID"],
        "system": build_system_prompt(os.getcwd()),
        "tools": make_tools(),
    }
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pytest s01_agent_loop/tests/test_config.py -v`
预期：3 passed

- [ ] **步骤 5：Commit**

```bash
git add s01_agent_loop/config.py s01_agent_loop/tests/test_config.py
git commit -m "feat(s01): 实现 config（env 加载 + 工具/系统提示）"
```

---

### 任务 5：cli + __main__（手动冒烟）

**文件：**
- 创建：`s01_agent_loop/cli.py`、`s01_agent_loop/__main__.py`

说明：cli 是 REPL 装配，靠手动冒烟验证（规格已定 REPL 不做单测）。

- [ ] **步骤 1：编写 cli.py**

```python
# s01_agent_loop/cli.py
"""交互式 REPL：把用户输入喂给 agent_loop，打印工具调用与最终回复。"""
try:
    import readline
    readline.parse_and_bind("set bind-tty-special-chars off")
    readline.parse_and_bind("set input-meta on")
    readline.parse_and_bind("set output-meta on")
    readline.parse_and_bind("set convert-meta off")
except ImportError:
    pass

from s01_agent_loop.agent import agent_loop
from s01_agent_loop.config import load
from s01_agent_loop.tools import run_bash


def print_tool_use(command: str, output: str) -> None:
    print(f"\033[33m$ {command}\033[0m")
    print(output[:200])


def main() -> None:
    cfg = load()
    print("s01: Agent Loop")
    print("输入问题，回车发送。输入 q 退出。\n")
    history: list = []
    while True:
        try:
            query = input("\033[36ms01 >> \033[0m")
        except (EOFError, KeyboardInterrupt):
            break
        if query.strip().lower() in ("q", "exit", ""):
            break
        history.append({"role": "user", "content": query})
        agent_loop(
            client=cfg["client"], model=cfg["model"], system=cfg["system"],
            tools=cfg["tools"], messages=history, run_tool=run_bash,
            on_tool_use=print_tool_use,
        )
        response_content = history[-1]["content"]
        if isinstance(response_content, list):
            for block in response_content:
                if getattr(block, "type", None) == "text":
                    print(block.text)
        print()
```

- [ ] **步骤 2：编写 __main__.py**

```python
# s01_agent_loop/__main__.py
from s01_agent_loop.cli import main

main()
```

- [ ] **步骤 3：手动冒烟**

运行：`source .venv/bin/activate && python -m s01_agent_loop`
在 `s01 >> ` 输入：`Create a file called hello.txt that says hi`
预期：黄色 `$ ...` 命令打印、工具输出、最终回复；输入 `q` 退出。
（依赖 `.env` 中有效的 `ANTHROPIC_API_KEY`/`MODEL_ID`/`ANTHROPIC_BASE_URL` —— 已配好。）

- [ ] **步骤 4：Commit**

```bash
git add s01_agent_loop/cli.py s01_agent_loop/__main__.py
git commit -m "feat(s01): 实现 REPL 入口（cli + __main__）"
```

---

### 任务 6：README + 全量测试 + 推送

**文件：**
- 创建：`s01_agent_loop/README.md`

- [ ] **步骤 1：编写 README**

````markdown
# s01: Agent Loop

po-agent 第一阶段，参照 `learn-claude-code/s01_agent_loop`。核心：一个 `while` 循环——模型调工具就执行并喂回结果、继续；不调就停。

## 结构
- `config.py` — env 加载、客户端、系统提示、工具定义
- `tools.py` — `run_bash`（危险黑名单 / 120s 超时 / 50000 截断）
- `agent.py` — `agent_loop` 核心循环（依赖注入，可单测）
- `cli.py` / `__main__.py` — 交互式 REPL

## 运行
```sh
source ../.venv/bin/activate
python -m s01_agent_loop
```

## 测试
```sh
pytest s01_agent_loop/tests -v
```
````

- [ ] **步骤 2：全量测试**

运行：`pytest s01_agent_loop/tests -v`
预期：11 passed（tools 5 + agent 3 + config 3）

- [ ] **步骤 3：Commit**

```bash
git add s01_agent_loop/README.md
git commit -m "docs(s01): 添加阶段 README"
```

- [ ] **步骤 4：推送**

运行：`git push`
预期：推送至 origin/main

---

## 自检

1. **规格覆盖：** tools/agent/config/cli/__main__/tests/README 均有任务；行为对齐（黑名单/超时/截断/stop_reason/tool_result/REPL）全覆盖。✓
2. **占位符扫描：** 无 TODO/待定，每步含实代码与命令。✓
3. **类型一致性：** `agent_loop` 签名（`client/model/system/tools/messages/run_tool/max_tokens/on_tool_use`）在测试与实现一致；`run_tool: Callable[[str],str]`、`on_tool_use: Callable[[str,str],None]` 一致；`load()` 返回 dict 键 `client/model/system/tools` 与 cli 解构一致；`run_bash` 返回 `str` 与 `run_tool` 签名一致。✓
